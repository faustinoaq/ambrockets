secret_key_base: wbFLRVuMeTzjwGhc_-FZCkOztwFv38q_3iyKmhvP-hU
port: (ENV["PORT"] || 3000).to_i
name: ambrockets
log: "::Logger.new(STDOUT)"
log_level: "::Logger::INFO"
color: false
host: 0.0.0.0
port_reuse: false
process_count: (ENV["AMBER_PROCESS_COUNT"]? || 1).to_i
ssl_key_file:
ssl_cert_file:
redis_url: "redis://localhost:6379"
session:
  key: amber.session
  store: :signed_cookie
  expires: 0
secrets:
  description: Store your development secrets credentials and settings here.
  database: postgres://postgres:@localhost:5432/ambrockets_development
